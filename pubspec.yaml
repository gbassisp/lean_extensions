name: lean_extensions
description: A collection of extension methods without flutter dependency
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  json_annotation: 

dev_dependencies:
  test: 
  very_good_analysis:
